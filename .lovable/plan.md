

## Plan: Scale Up "Show Up Here" Logo Section

### Changes to `src/components/HeroSection.tsx` (lines 75-81)

- Increase the "Show Up Here:" label from `text-[13px]` to `text-[26px]`
- Increase gap between label and logos from `gap-4` to `gap-8`
- Increase margin top from `mt-4` to `mt-8`
- Increase gap between logos from `gap-10` to `gap-16`
- Set all three logo images to the same height: `h-14` (uniform size, ~2x the current `h-7`/`h-8`)

